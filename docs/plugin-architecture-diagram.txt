```
╔═══════════════════════════════════════════════════════════════════════════╗
║                    CoolerControl Plugin-Architektur                        ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│  INTEGRATION Plugin (coolerdash - AKTUELL ✅)                            │
└─────────────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │ CoolerDash   │ (Eigenständiger C-Daemon)
    │   Daemon     │
    │  (C99)       │
    └──────┬───────┘
           │
           │ 1. Login (HTTP Basic Auth)
           │ 2. GET /devices (Device UID holen)
           │ 3. POST /status (Temperaturen lesen)
           │ 4. PUT /devices/{uid}/settings/lcd/lcd/images (PNG hochladen)
           │
           ▼
    ┌──────────────────────┐
    │  CoolerControl       │  REST API
    │  (coolercontrold)    │  Port 11987
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │   Hardware (LCD)     │  NZXT Kraken, etc.
    └──────────────────────┘

    ⏱️  Update-Frequenz: Vom Daemon gesteuert (config.ini)
    📁 Config: /etc/coolercontrol/plugins/coolerdash/config.ini
    🔐 Privileged: true (für Hardware-Zugriff)


┌─────────────────────────────────────────────────────────────────────────┐
│  DEVICE SERVICE Plugin (Alternative - NICHT IMPLEMENTIERT)               │
└─────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────┐
    │  CoolerControl       │  gRPC Client (pollt jede Sekunde)
    │  (coolercontrold)    │
    └──────────┬───────────┘
               │
               │ Unix Domain Socket: /run/coolercontrol-plugin-coolerdash.sock
               │
               │ gRPC Calls:
               │ - ListDevices()  → Welche Devices hat dein Service?
               │ - Status()       → Aktuelle Temps/Speeds (jede Sekunde!)
               │ - Lcd()          → LCD-Bild setzen
               │ - Shutdown()     → Graceful Shutdown
               │
               ▼
    ┌──────────────────────────┐
    │  CoolerDash              │  gRPC Server (Rust/C++)
    │  Device Service          │
    │  (Rust + Protobuf)       │
    └──────────┬───────────────┘
               │
               ▼
    ┌──────────────────────┐
    │   Hardware (LCD)     │  NZXT Kraken, etc.
    └──────────────────────┘

    ⏱️  Update-Frequenz: Von CoolerControl gesteuert (~1s)
    📁 Config: gRPC Messages (JSON via Plugin-Config)
    🔐 Privileged: true (für Hardware-Zugriff)


╔═══════════════════════════════════════════════════════════════════════════╗
║                         Datenfluss-Vergleich                               ║
╚═══════════════════════════════════════════════════════════════════════════╝

INTEGRATION (aktuell):
┌─────────────┐     REST      ┌──────────────┐     USB/HID    ┌─────────┐
│ CoolerDash  │───────────────>│ CoolerControl│───────────────>│  Kraken │
│   Daemon    │<───────────────│              │<───────────────│   LCD   │
└─────────────┘   (CURL GET)   └──────────────┘   (Liquidctl)  └─────────┘
      ↑
      │ Steuert selbst:
      └─ Refresh-Intervall (2.5s)
      └─ Rendering (Cairo)
      └─ Upload-Timing


DEVICE SERVICE (Alternative):
┌──────────────┐    gRPC/UDS   ┌─────────────┐    USB/HID     ┌─────────┐
│ CoolerControl│──────────────>│  CoolerDash │───────────────>│  Kraken │
│              │<──────────────│   Service   │<───────────────│   LCD   │
└──────────────┘ (Status Poll) └─────────────┘  (Liquidctl?)  └─────────┘
      ↑
      │ CoolerControl steuert:
      └─ Poll-Frequenz (1s fest)
      └─ Device-Lifecycle
      └─ UI-Integration


╔═══════════════════════════════════════════════════════════════════════════╗
║                    Wann welcher Typ sinnvoll ist                           ║
╚═══════════════════════════════════════════════════════════════════════════╝

INTEGRATION ✅ (Empfohlen für coolerdash):
┌────────────────────────────────────────────────────────────────────────┐
│ ✅ Eigenständiger Workflow (eigene Timing-Kontrolle)                   │
│ ✅ Nutzt existierende CoolerControl-Devices                            │
│ ✅ Einfache REST API (CURL in C)                                       │
│ ✅ Keine gRPC-Komplexität                                              │
│ ✅ Performant (kein Polling-Overhead)                                  │
│                                                                         │
│ Beispiele:                                                              │
│ • LCD Dashboard (coolerdash)                                            │
│ • RGB Sync mit externer Software                                       │
│ • Custom Monitoring/Logging                                            │
│ • Integration mit Home Assistant, MQTT, etc.                           │
└────────────────────────────────────────────────────────────────────────┘


DEVICE SERVICE 🔧 (Für virtuelle Hardware):
┌────────────────────────────────────────────────────────────────────────┐
│ ✅ Erstellt virtuelles Gerät in CoolerControl                          │
│ ✅ Channels werden als native Sensoren/Lüfter behandelt               │
│ ✅ Vollständige UI-Integration (Profiles, Functions)                   │
│ ✅ CoolerControl managed Lifecycle                                     │
│ ❌ gRPC/Protobuf erforderlich (Rust empfohlen)                         │
│ ❌ Polled von CoolerControl (fixe 1s Frequenz)                         │
│                                                                         │
│ Beispiele:                                                              │
│ • Custom-Device Plugin (Shell-Commands für HW)                         │
│ • Software-basierte Sensoren (Z.B. NVMe via smartctl)                 │
│ • Virtuelle Lüfter-Controller                                          │
│ • Network-Remote Devices                                               │
└────────────────────────────────────────────────────────────────────────┘


╔═══════════════════════════════════════════════════════════════════════════╗
║                      Technologie-Vergleich                                 ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────┬────────────────────┬───────────────────────┐
│                     │   INTEGRATION      │   DEVICE SERVICE      │
├─────────────────────┼────────────────────┼───────────────────────┤
│ Sprache             │ C, Python, Bash... │ Rust (empfohlen)      │
│ Kommunikation       │ REST (HTTP/CURL)   │ gRPC (Unix Socket)    │
│ Komplexität         │ ⭐ Einfach         │ ⭐⭐⭐ Komplex        │
│ Proto/Codegen       │ ❌ Nicht nötig     │ ✅ Erforderlich       │
│ Dependencies        │ libcurl, jansson   │ tonic, prost, tokio   │
│ Build-System        │ Make, gcc          │ Cargo, protoc         │
│ Status-Reporting    │ ❌ Optional        │ ✅ Erforderlich       │
│ UI-Integration      │ Config-Seite       │ Volles Device-UI      │
│ Lifecycle-Kontrolle │ Selbst managed     │ CoolerControl managed │
└─────────────────────┴────────────────────┴───────────────────────┘


╔═══════════════════════════════════════════════════════════════════════════╗
║                    manifest.toml Unterschiede                              ║
╚═══════════════════════════════════════════════════════════════════════════╝

INTEGRATION (coolerdash):
┌────────────────────────────────────────────────────────────────────────┐
│ [manifest.toml]                                                         │
│ id = "coolerdash"                                                       │
│ type = "integration"              ← Kein Socket!                        │
│ executable = ".../coolerdash"     ← Eigenständiger Daemon               │
│ args = "--verbose"                                                      │
│ privileged = true                 ← Root für Hardware                   │
│ # address = (nicht gesetzt)       ← Keine Socket-Kommunikation         │
└────────────────────────────────────────────────────────────────────────┘


DEVICE SERVICE:
┌────────────────────────────────────────────────────────────────────────┐
│ [manifest.toml]                                                         │
│ id = "my-device-service"                                                │
│ type = "device"                   ← gRPC Server!                        │
│ executable = ".../my-service"     ← gRPC Service Binary                 │
│ address = "/run/cc-plugin-my.sock" ← Unix Domain Socket                │
│ privileged = true                 ← Root für Hardware                   │
│                                                                         │
│ → CoolerControl connected zu diesem Socket                             │
└────────────────────────────────────────────────────────────────────────┘


╔═══════════════════════════════════════════════════════════════════════════╗
║                         Empfehlung für coolerdash                          ║
╚═══════════════════════════════════════════════════════════════════════════╝

    ✅ BLEIB bei type = "integration"

    Gründe:
    1. Funktioniert bereits perfekt
    2. C99 ist ideal für embedded-ähnliche Tasks
    3. Volle Kontrolle über Timing/Rendering
    4. Keine gRPC/Rust-Komplexität
    5. Performanter (kein 1s Polling-Overhead)

    Verbessere stattdessen:
    • Plugin-Config-API (JSON statt INI)
    • UI-Einstellungen (savePluginConfig/getPluginConfig)
    • Fehlerbehandlung/Logging
    • Desktop-Shortcut für Einstellungen

    Device Service wäre nur sinnvoll wenn:
    ❌ Du willst ein virtuelles Device erstellen
    ❌ Du willst Channels in Profiles nutzen
    ❌ Du bist bereit alles neu zu schreiben

```
